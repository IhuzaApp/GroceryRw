query getShopsByCategory($categoryId: uuid!) {
  Shops(where: {category_id: {_eq: $categoryId}, is_active: {_eq: true}}) {
    id
    name
    image
    description
    category_id
    is_active
    latitude
    longitude
    operating_hours
    updated_at
    address
    created_at
  }
}
